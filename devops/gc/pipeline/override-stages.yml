variables:
  GC_SERVICE: search
  GC_VENDOR: gc
  GC_TESTING_DIR: "testing/integration-tests"

gc-test:
  variables:
    CUCUMBER_OPTIONS: "--tags @autocomplete,~@ignore"

gc-dev2-test:
  variables:
    CUCUMBER_OPTIONS: "--tags @autocomplete,~@ignore"
